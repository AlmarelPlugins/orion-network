# orion-network
Read-only remote configuration distribution for Orion plugin (version manifests, notices). Statically served JSON, consumed programmatically at runtime. Not intended for external use/forking.
